# Diagram Assets

Markdown files reference the SVG files in this directory instead of embedding
Mermaid code blocks directly. This keeps diagrams identical between local
preview and GitHub, whose Mermaid renderer can choose a different layout.

The matching `.mmd` files are the Mermaid sources used to regenerate each SVG.
